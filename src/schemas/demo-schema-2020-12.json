{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "http://scanning-electron-microscopy-2020-12",
    "title": "SEM Experiment — 2020-12",
    "description": "Demo schema for JSON Schema Draft 2020-12. Highlights: '$defs' and honored $ref siblings (same as 2019-09), exclusiveMinimum/Maximum as standalone numbers (same as 2019-09), 'dependentRequired' (same as 2019-09), 'unevaluatedProperties' (same as 2019-09). Key difference from 2019-09: tuple syntax changes from 'items-array' to 'prefixItems'.",
    "type": "object",
    "required": ["DeviceModel", "Operator", "SEMParameters"],
    "unevaluatedProperties": false,
    "properties": {
        "DeviceModel": {
            "title": "Device Model",
            "description": "SEM device model used in the experiment.",
            "type": "string",
            "enum": ["FEI Quanta 650", "Zeiss Sigma 300", "Hitachi SU8230"]
        },
        "Operator": {
            "title": "Operator Name",
            "description": "Full name of the operator running the instrument.",
            "type": "string",
            "minLength": 2,
            "maxLength": 60
        },
        "SEMParameters": {
            "title": "SEM Parameters",
            "description": "Acquisition parameters set on the instrument.",
            "type": "object",
            "required": ["AccelerationVoltage"],
            "unevaluatedProperties": false,
            "properties": {
                "AccelerationVoltage": {
                    "$ref": "#/$defs/voltage",
                    "title": "Acceleration Voltage [kV]",
                    "description": "2020-12: $ref and sibling keywords are merged (same behavior as 2019-09). Both this field's 'minimum: 10' AND the $defs/voltage constraints are applied together.",
                    "type": "number",
                    "minimum": 10
                },
                "ProbeCurrent": {
                    "title": "Probe Current [nA]",
                    "description": "2020-12 (same as 2019-09): 'exclusiveMinimum' is a standalone number. Value must be strictly greater than 0.",
                    "type": "number",
                    "exclusiveMinimum": 0
                },
                "BeamEnergy": {
                    "title": "Beam Energy [eV]",
                    "description": "2020-12 (same as 2019-09): 'exclusiveMaximum' is a standalone number. Value must be strictly less than 30000. 'multipleOf' constrains value to increments of 100.",
                    "type": "number",
                    "minimum": 100,
                    "exclusiveMaximum": 30000,
                    "multipleOf": 100
                },
                "WorkingDistance": {
                    "title": "Working Distance [mm]",
                    "description": "Distance from the final lens pole piece to the specimen surface. Providing this field makes ProbeCurrent required (see 'dependentRequired' on the parent object).",
                    "type": "number",
                    "minimum": 1,
                    "maximum": 50
                },
                "ProbeDiameter": {
                    "title": "Probe Diameter [nm]",
                    "description": "Estimated diameter of the electron probe at the specimen surface. Plain inclusive minimum — contrast with ProbeCurrent which uses an exclusive minimum.",
                    "type": "number",
                    "minimum": 0
                },
                "DetectorMode": {
                    "title": "Detector Mode",
                    "description": "Secondary electron (SE), backscattered electron (BSE), or STEM detector mode.",
                    "type": "string",
                    "enum": ["SE", "BSE", "STEM"]
                },
                "SampleLabel": {
                    "title": "Sample Label",
                    "description": "Alphanumeric identifier for the sample. Pattern enforced via regex.",
                    "type": "string",
                    "pattern": "^[A-Za-z0-9_-]+$"
                },
                "ScanSequence": {
                    "title": "Scan Sequence (Tuple)",
                    "description": "2020-12 tuple syntax: 'items-array' is replaced by 'prefixItems'. This is the only tuple-related change from 2019-09. Position 0 = dwell time, 1 = line count, 2 = direction.",
                    "type": "array",
                    "prefixItems": [
                        { "type": "number", "title": "Dwell time [µs]" },
                        { "type": "integer", "title": "Line count" },
                        { "type": "string", "title": "Scan direction" }
                    ]
                }
            },
            "dependentRequired": {
                "WorkingDistance": ["ProbeCurrent"]
            }
        }
    },
    "$defs": {
        "voltage": {
            "type": "number",
            "minimum": 0,
            "maximum": 30
        }
    }
}
